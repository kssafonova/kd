"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { RemoteImage } from "../../../remote-image";
import { ScenarioConstructor } from "../../../constructor/scenario-client";
import { loadConstructorData } from "../../../constructor/data-client";
import { getEditorialScenario } from "../../../editorial-scenario-config";
import type { CandidateRow, CatalogRow, ConstructorData, ExpansionRuleRow, PresetRow, ScenarioMetaRow } from "../../../constructor/types";
import { ExpansionScenarioBuilder } from "./expansion-builder";
import styles from "../../../editorial-scenarios.module.css";

const EVENT_LABELS:Record<string,string>={tea:"Чаепитие",breakfast:"Завтрак",brunch:"Бранч",lunch:"Обед",dinner:"Ужин",celebration:"Праздник",anniversary:"Юбилей",new_year:"Новый год",gift:"Подарок",housewarming:"Новоселье",romantic_evening:"Романтический вечер",family:"Семейный стол",guests:"Гости",reading:"Чтение",evening:"Вечер дома",bedroom:"Спальня",bedroom_refresh:"Обновление спальни",rest:"Отдых",self_care:"Self-care"};
const unique=<T,>(values:T[])=>Array.from(new Set(values));
const splitPipe=(value:string)=>value.split("|").map(item=>item.trim()).filter(Boolean);
type StoryProduct={offerId:string;name:string;collection:string;image:string;productType:string};
const fromPreset=(row:PresetRow):StoryProduct=>({offerId:row.offer_id,name:row.product_name,collection:row.collection,image:row.primary_image_url,productType:row.product_type});
const fromCandidate=(row:CandidateRow):StoryProduct=>({offerId:row.offer_id,name:row.product_name,collection:row.collection,image:row.primary_image_url,productType:row.product_type});
const fromCatalog=(row:CatalogRow):StoryProduct=>({offerId:row.offer_id,name:row.product_name,collection:row.collection,image:row.primary_image_url,productType:row.product_type});

function patchedExpansionRules(data:ConstructorData,scenarioId:string){return data.expansionRules.filter(row=>row.scenario_id===scenarioId).map(row=>{const patch=data.expansionPatches.find(item=>item.scenario_id===row.scenario_id&&item.role===row.role);return patch?{...row,allowed_product_types:patch.allowed_product_types,preset_status:patch.preset_status}:row})}
function coreScenarioRules(data:ConstructorData,meta:ScenarioMetaRow[]):ExpansionRuleRow[]{return meta.map(row=>{const collections=unique([...splitPipe(row.entry_collection),...splitPipe(row.allowed_collections)]);const productTypes=unique(data.catalog.filter(product=>product.constructor_role===row.role&&collections.includes(product.collection)).map(product=>product.product_type).filter(Boolean));const normalizedStatus=row.preset_status==="required_or_default"?"required":row.preset_status;return{scenario_id:row.scenario_id,scenario_name:row.scenario_name,space:"",occasion:row.occasion,guests_supported:row.guests_supported,lead_collections:row.entry_collection,allowed_collections:collections.join("|"),role:row.role,allowed_product_types:productTypes.join("|"),preset_status:normalizedStatus,quantity_rule:row.quantity_rule,flow_step:normalizedStatus==="required"?"Выберите основу":"Добавьте предметы",styling_message:row.styling_message}})}
function fallbackCatalog(data:ConstructorData,meta:ScenarioMetaRow[]){const collections=unique(meta.flatMap(row=>`${row.entry_collection}|${row.allowed_collections}`.split("|")).map(value=>value.trim()).filter(Boolean));return data.catalog.filter(row=>collections.includes(row.collection)).slice(0,8).map(fromCatalog)}
function ruleCatalog(data:ConstructorData,rules:ExpansionRuleRow[]){const products=rules.flatMap(rule=>{const collections=splitPipe(rule.allowed_collections);const productTypes=splitPipe(rule.allowed_product_types).filter(item=>item!=="required");return data.catalog.filter(row=>collections.includes(row.collection)&&productTypes.includes(row.product_type))});const seen=new Set<string>();return products.filter(row=>{if(!row.offer_id||seen.has(row.offer_id))return false;seen.add(row.offer_id);return true}).slice(0,8).map(fromCatalog)}
function getStoryProducts(data:ConstructorData,scenarioId:string,meta:ScenarioMetaRow[],rules:ExpansionRuleRow[]){const presetProducts=data.presets.filter(row=>row.scenario_id===scenarioId).map(fromPreset);if(presetProducts.length)return presetProducts;const candidateProducts=data.candidates.filter(row=>row.scenario_id===scenarioId).map(fromCandidate);if(candidateProducts.length){const seen=new Set<string>();return candidateProducts.filter(item=>{if(seen.has(item.offerId))return false;seen.add(item.offerId);return true}).slice(0,8)}if(rules.length)return ruleCatalog(data,rules);return fallbackCatalog(data,meta)}

export function EditorialScenarioStory({scenarioId}:{scenarioId:string}){
  const config=getEditorialScenario(scenarioId);const[data,setData]=useState<ConstructorData|null>(null);const[error,setError]=useState("");
  useEffect(()=>{let active=true;loadConstructorData().then(loaded=>active&&setData(loaded)).catch((reason:unknown)=>active&&setError(reason instanceof Error?reason.message:"Не удалось загрузить данные сценария"));return()=>{active=false}},[scenarioId]);
  const meta=useMemo(()=>(data?.scenarios??[]).filter(row=>row.scenario_id===scenarioId),[data,scenarioId]);
  const expansionRules=useMemo(()=>(data?patchedExpansionRules(data,scenarioId):[]),[data,scenarioId]);
  const coreRules=useMemo(()=>(data?coreScenarioRules(data,meta):[]),[data,meta]);
  const isExpansion=config?.source==="expansion";
  const builderRules=isExpansion?expansionRules:coreRules;
  const products=useMemo(()=>(data?getStoryProducts(data,scenarioId,meta,builderRules):[]),[data,meta,scenarioId,builderRules]);
  const hasPreset=Boolean(data?.presets.some(row=>row.scenario_id===scenarioId));
  const occasions=unique((isExpansion?expansionRules.flatMap(row=>splitPipe(row.occasion)):meta.flatMap(row=>splitPipe(row.occasion))).filter(Boolean));
  const styling=unique(isExpansion?expansionRules.map(row=>row.styling_message).filter(Boolean):meta.map(row=>row.styling_message).filter(Boolean)).join(". ");
  const heroImages=unique(products.map(product=>product.image).filter(Boolean)).slice(0,3);
  const hasRuleBuilder=!hasPreset&&builderRules.length>0;
  if(!config)return null;
  return <div className={styles.storyShell}><header className={styles.storyTopbar}><Link className={styles.storyBrand} href="/">КУЛЬТУРА ДОМА</Link><Link className={styles.storyBack} href="/">← Вернуться к Editorial</Link></header><section className={styles.storyHero}><div className={styles.storyHeroCopy}><span className={styles.storyKicker}>{config.spaceLabel} · EDITORIAL STORY</span><h1>{config.name}</h1><p className={styles.storyHeroLead}>{config.lead}</p><div className={styles.storyHeroMeta}>{occasions.map(occasion=><span key={occasion}>{EVENT_LABELS[occasion]||occasion}</span>)}<span>{hasPreset?"Готовый preset":isExpansion?"Конструктор по expansion rules":"Конструктор по scenario rules"}</span></div>{(hasPreset||hasRuleBuilder)&&<a className={styles.storyPrimaryLink} href="#builder">Собрать свою капсулу</a>}</div><div className={styles.storyHeroMedia}>{heroImages.length?heroImages.map((image,index)=><RemoteImage key={`${image}-${index}`} src={image} alt={`${config.name}, предмет ${index+1}`} loading={index===0?"eager":"lazy"}/>):<div className={styles.scenarioFallback}>В master catalog нет изображения для разрешённого состава</div>}</div></section><section className={styles.storySection}><header className={styles.storySectionHead}><h2>История пространства</h2><p>{styling||config.lead} Сценарий строится вокруг конкретного пространства, повода и совместимого набора предметов — без перехода в общий каталог.</p></header><div className={styles.storyRules}><article className={styles.storyRule}><small>Что собираем</small><p>{config.canInclude}</p></article><article className={styles.storyRule}><small>Границы сценария</small><p>{config.exclude}</p></article></div></section><section className={styles.storySection}><header className={styles.storySectionHead}><h2>Предметы истории</h2><p>{hasPreset?"Состав начинается с финального preset и дальше настраивается только совместимыми заменами.":"Показаны только реальные товары master catalog, прошедшие правила текущего сценария. Конкретный offer_id выбирает пользователь — случайный preset не создаётся."}</p></header><div className={styles.storyProducts}>{products.slice(0,8).map(product=><article className={styles.storyProduct} key={`${product.offerId}-${product.name}`}>{product.image?<RemoteImage src={product.image} alt={`${product.name}, ${product.collection}`} loading="lazy"/>:<div className={styles.scenarioFallback}>Фото недоступно</div>}<div><small>{product.collection||product.productType}</small><strong>{product.name}</strong></div></article>)}</div></section>{error&&<div className={styles.storyPending}><small>ДАННЫЕ</small><h2>Не удалось загрузить CSV</h2><p>{error}</p></div>}{!error&&hasPreset&&<section id="builder"><div className={styles.storySection}><div className={styles.storyBuilderIntro}><span className={styles.storyKicker}>НАСТРОИТЬ ПОД СЕБЯ</span><h2>Соберите свою капсулу</h2><p>Меняйте только совместимые позиции, выбирайте количество персон или размер и сразу видите состав и итоговую стоимость.</p></div></div><ScenarioConstructor scenarioId={scenarioId}/></section>}{!error&&data&&hasRuleBuilder&&<section id="builder" className={styles.storySection}><div className={styles.storyBuilderIntro}><span className={styles.storyKicker}>СОБРАТЬ ПО ПРАВИЛАМ СЦЕНАРИЯ</span><h2>Соберите свою капсулу</h2><p>{isExpansion?"Preset SKU намеренно не выдуман. Выберите реальные товары для каждой роли из master catalog; интерфейс ограничивает выбор разрешёнными коллекциями и product_type.":"Для сценария без final preset выбор строится по его role, allowed_collections и классификации constructor_role master catalog. Конкретный SKU выбираете вы."}</p></div><ExpansionScenarioBuilder scenarioId={scenarioId} rules={builderRules} catalog={data.catalog}/></section>}{!error&&data&&!hasPreset&&!hasRuleBuilder&&<div className={styles.storyPending}><small>НЕТ ДОСТАТОЧНЫХ ПРАВИЛ</small><h2>Сценарий пока нельзя собрать</h2><p>В CSV недостаточно данных для безопасного выбора реальных SKU. Товары не подменяются случайными позициями.</p></div>}</div>
}
